using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Gerard.Tests
{
   [TestClass]
   public class TestPlayerCache
   {
      [TestMethod]
      public void TestGet()
      {
         //var sut = new PlayerCache();
         //var p = new NFLPlayer( "MONTJO01" );
         //sut.Put( p );
         //var ch = sut.Get( p.PlayerCode );
         //Assert.IsTrue( ch.Surname.Equals( "Montana" ) );
      }
   }
}
